{
  "name": "--yes",
  "version": "1.0.0",
  "description": "## NPM ## To initialize with npm, the command is 'npm init'   -Makes a package.json   -To answer all the questions 'yes', command is 'npm init --yes\"",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
